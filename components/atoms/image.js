
export default function AtomImage({ src, width, height }) {
    return (

        <img style={{
            src: src,
            width: width,
            height: height,
        }}>

        </img>

    );
}